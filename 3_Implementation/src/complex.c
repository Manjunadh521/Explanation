#include "complex.h"


float a3_b3 (float a,float b){ // function defination
    
    return pow((a+b),3);  // pow(base,power);
}

//(a-b)^3

float S_a3_b3 (float a,float b){
    return pow((a-b),3);
}

//(a+b)^4

float a4_b4(float a, float b){
    return pow((a+b),4);
}

//(a+b)^5

float a5_b5(float a, float b){
    return pow((a+b),5);
}
